<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tauqeer Mustafa</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&family=Inter:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500&display=swap');

        :root {
            --accent: #d97706; /* Amber 600 - Visible Gold */
            --accent-glow: rgba(217, 119, 6, 0.3);
            --dark-text: #0f172a;
            --light-bg: #ffffff;
            --grid-color: rgba(0, 0, 0, 0.05);
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--light-bg);
            color: var(--dark-text);
            overflow-x: hidden;
            cursor: none; /* Custom cursor */
        }

        /* --- CUSTOM CURSOR --- */
        .cursor-dot {
            width: 8px; height: 8px;
            background-color: var(--accent);
            position: fixed; top: 0; left: 0;
            transform: translate(-50%, -50%);
            border-radius: 50%; z-index: 9999; pointer-events: none;
        }
        .cursor-outline {
            width: 40px; height: 40px;
            border: 1px solid rgba(0, 0, 0, 0.2);
            position: fixed; top: 0; left: 0;
            transform: translate(-50%, -50%);
            border-radius: 50%; z-index: 9999; pointer-events: none;
            transition: width 0.2s, height 0.2s, background-color 0.2s;
        }
        body:hover .cursor-outline {
            background-color: rgba(217, 119, 6, 0.05);
            border-color: var(--accent);
        }

        h1, h2, h3, .brand-font { font-family: 'Space Grotesk', sans-serif; }
        .mono-font { font-family: 'JetBrains Mono', monospace; }
        
        /* Custom Scrollbar */
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: #f1f5f9; }
        ::-webkit-scrollbar-thumb { background: var(--accent); border-radius: 10px; }

        /* --- REACTOR ANIMATION --- */
        .reactor-container {
            position: absolute;
            top: 50%; left: 50%;
            transform: translate(-50%, -50%);
            width: 800px; height: 800px;
            z-index: -1;
            opacity: 0.6;
            pointer-events: none;
        }
        .reactor-ring {
            position: absolute;
            border-radius: 50%;
            border: 1px solid rgba(0, 0, 0, 0.1);
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.05);
        }
        .r1 { width: 100%; height: 100%; animation: spin 30s linear infinite; border-width: 1px; border-style: dashed; border-color: #cbd5e1; }
        .r2 { width: 70%; height: 70%; top: 15%; left: 15%; animation: spin-reverse 20s linear infinite; border-color: #94a3b8; }
        .r3 { width: 40%; height: 40%; top: 30%; left: 30%; animation: spin 10s linear infinite; border: 2px solid var(--accent); box-shadow: 0 0 30px var(--accent-glow); }
        
        @keyframes spin { 100% { transform: rotate(360deg); } }
        @keyframes spin-reverse { 100% { transform: rotate(-360deg); } }

        /* --- ROTATING BORDER --- */
        .rotating-border { position: relative; z-index: 0; overflow: hidden; }
        .rotating-border::before {
            content: ''; position: absolute; z-index: -2; left: -50%; top: -50%; width: 200%; height: 200%;
            background-image: linear-gradient(var(--accent), #000);
            animation: rotate 4s linear infinite;
        }
        .rotating-border::after {
            content: ''; position: absolute; z-index: -1; left: 2px; top: 2px; width: calc(100% - 4px); height: calc(100% - 4px);
            background: #fff; border-radius: 8px;
        }
        @keyframes rotate { 100% { transform: rotate(1turn); } }

        /* --- SCANLINE CARD --- */
        .scanline-card { position: relative; overflow: hidden; background: white; }
        .scanline-card::after {
            content: ""; position: absolute; top: -100%; left: 0; width: 100%; height: 20px;
            background: linear-gradient(to bottom, transparent, rgba(0, 0, 0, 0.1), transparent);
            opacity: 0; transition: 0.3s;
        }
        .scanline-card:hover::after { opacity: 1; animation: scan 2s linear infinite; }
        @keyframes scan { 0% { top: -20%; } 100% { top: 120%; } }

        /* --- GLASSMORPHISM --- */
        .glass {
            background: rgba(255, 255, 255, 0.7);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(0, 0, 0, 0.05);
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
        }

        /* --- HOLOGRAPHIC ID CARD --- */
        .holo-card {
            background: #ffffff;
            border: 1px solid #e2e8f0;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            position: relative; overflow: hidden; transition: all 0.5s ease;
        }
        .holo-card::before {
            content: ''; position: absolute; top: 0; left: -100%; width: 50%; height: 100%;
            background: linear-gradient(90deg, transparent, rgba(217, 119, 6, 0.1), transparent);
            transform: skewX(-25deg); transition: 0.5s; pointer-events: none;
        }
        .holo-card:hover::before { left: 150%; transition: 0.7s; }
        .scan-line {
            position: absolute; top: 0; left: 0; width: 100%; height: 2px;
            background: var(--accent);
            box-shadow: 0 0 10px var(--accent);
            animation: scan-vertical 3s linear infinite; opacity: 0.5;
        }
        @keyframes scan-vertical { 0% { top: 0%; opacity: 0; } 10% { opacity: 1; } 90% { opacity: 1; } 100% { top: 100%; opacity: 0; } }

        /* --- UTILS --- */
        .text-glow { text-shadow: 0 0 20px rgba(217, 119, 6, 0.4); }
        .hacker-text { opacity: 0; }
        .hacker-text.visible { opacity: 1; }
        
        /* Marquee */
        @keyframes scroll { 0% { transform: translateX(0); } 100% { transform: translateX(-50%); } }
        .animate-scroll { animation: scroll 30s linear infinite; }

        /* AI Chat */
        .ai-modal {
            position: fixed; bottom: 100px; right: 30px; width: 350px; height: 500px;
            background: white; border: 1px solid #e2e8f0; border-radius: 12px;
            z-index: 1000; display: flex; flex-direction: column;
            box-shadow: 0 20px 50px rgba(0,0,0,0.15);
            transform: scale(0.9); opacity: 0; pointer-events: none; transition: all 0.3s ease;
        }
        .ai-modal.active { opacity: 1; pointer-events: all; transform: scale(1); }
        
        /* Accordion */
        .accordion-item { border-bottom: 1px solid #e2e8f0; }
        .accordion-header { cursor: pointer; padding: 1.5rem 0; display: flex; justify-content: space-between; align-items: center; }
        .accordion-content { max-height: 0; overflow: hidden; transition: max-height 0.3s ease-out; opacity: 0.7; }
        .accordion-item.active .accordion-content { max-height: 200px; padding-bottom: 1.5rem; opacity: 1; }
        .accordion-icon { transition: transform 0.3s ease; }
        .accordion-item.active .accordion-icon { transform: rotate(180deg); }

        /* Selection */
        ::selection { background: var(--accent); color: white; }
    </style>
</head>
<body class="selection:bg-amber-500 selection:text-white">

    <!-- CUSTOM CURSOR -->
    <div class="cursor-dot"></div>
    <div class="cursor-outline"></div>

    <!-- BACKGROUNDS -->
    <div class="fixed inset-0 bg-[radial-gradient(#e5e7eb_1px,transparent_1px)] [background-size:20px_20px] opacity-50 -z-50"></div>
    <canvas id="canvas" class="fixed top-0 left-0 w-full h-full -z-40 opacity-30"></canvas>

    <!-- Navigation -->
    <nav class="fixed w-full z-50 glass transition-all duration-300 border-b border-slate-200" id="navbar">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-24">
                <div class="flex items-center gap-4 cursor-pointer group" onclick="window.scrollTo(0,0)">
                    <div class="relative w-12 h-12 flex items-center justify-center bg-white border border-amber-500 rounded-xl overflow-hidden group-hover:scale-110 transition-transform duration-300 shadow-lg shadow-amber-500/20">
                        <span class="font-bold text-amber-600 text-xl brand-font relative z-10">TM</span>
                    </div>
                    <div>
                        <span class="font-bold text-xl leading-none block text-slate-900 group-hover:text-amber-600 transition-colors tracking-wide">Tauqeer M.</span>
                        <span class="text-[10px] text-slate-500 uppercase tracking-[0.2em] font-bold block mt-1">SecOps & Finance</span>
                    </div>
                </div>

                <div class="hidden md:flex items-center space-x-10">
                    <a href="#expertise" class="text-sm font-bold text-slate-500 hover:text-slate-900 transition-all uppercase tracking-widest hover:scale-110 duration-300">Expertise</a>
                    <a href="#projects" class="text-sm font-bold text-slate-500 hover:text-slate-900 transition-all uppercase tracking-widest hover:scale-110 duration-300">Projects</a>
                    <a href="#about" class="text-sm font-bold text-slate-500 hover:text-slate-900 transition-all uppercase tracking-widest hover:scale-110 duration-300">About</a>
                    
                    <!-- Rotating Border Button -->
                    <div class="rotating-border rounded-lg p-[1px] group cursor-none">
                        <a href="#contact" class="relative block bg-slate-900 text-white px-8 py-3 rounded-lg font-bold uppercase tracking-wider hover:bg-amber-500 hover:text-white transition-colors z-10">
                            Get In Touch
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="min-h-screen flex items-center justify-center pt-20 relative px-4 overflow-hidden">
        
        <!-- REACTOR CORE ANIMATION (Visible on White) -->
        <div class="reactor-container">
            <div class="reactor-ring r1"></div>
            <div class="reactor-ring r2"></div>
            <div class="reactor-ring r3"></div>
        </div>

        <div class="max-w-7xl mx-auto text-center relative z-10">
            <div class="inline-flex items-center gap-3 mb-10 px-6 py-2 rounded-full border border-slate-200 bg-white/80 backdrop-blur-xl shadow-lg animate-bounce">
                <span class="w-2 h-2 rounded-full bg-green-500 animate-pulse"></span>
                <span class="text-slate-600 text-xs font-bold tracking-[0.2em] uppercase">Turning dreams into reality</span>
            </div>
            
            <h1 class="text-6xl md:text-8xl lg:text-9xl font-black mb-8 tracking-tighter text-slate-900">
                <span class="hacker-text" data-value="TAUQEER">TAUQEER</span><br>
                <span class="hacker-text text-amber-600 text-glow" data-value="MUSTAFAA">MUSTAFA</span>
            </h1>
            
            <div class="h-24 flex flex-col items-center justify-center gap-2 mb-8">
                 <div class="w-1 h-12 bg-gradient-to-b from-transparent via-amber-500 to-transparent"></div>
                 <p class="text-xl md:text-2xl text-slate-600 font-light tracking-wide">
                     The Intersection of <span class="font-bold text-slate-900 bg-slate-100 px-2 py-0.5 rounded">Security</span> & <span class="font-bold text-slate-900 bg-slate-100 px-2 py-0.5 rounded">Finance</span>
                 </p>
            </div>

            <div class="flex flex-col sm:flex-row gap-8 justify-center mt-12">
                <a href="#projects" class="group relative px-10 py-5 bg-amber-500 text-white font-black text-lg uppercase tracking-wider rounded-none skew-x-[-10deg] hover:bg-slate-900 transition-colors duration-300 shadow-xl shadow-amber-500/30">
                    <span class="block skew-x-[10deg]">View Operations</span>
                </a>
                <a href="#contact" class="group relative px-10 py-5 bg-transparent border-2 border-slate-900 text-slate-900 font-black text-lg uppercase tracking-wider rounded-none skew-x-[-10deg] hover:bg-slate-50 transition-colors duration-300">
                    <span class="block skew-x-[10deg] flex items-center gap-2">Contact Us <i data-lucide="chevron-right" class="w-5 h-5"></i></span>
                </a>
            </div>
        </div>
    </section>

    <!-- Stats Bar -->
    <section class="border-y border-slate-200 bg-white relative z-20">
        <div class="max-w-7xl mx-auto px-4">
            <div class="grid grid-cols-2 md:grid-cols-4 divide-x divide-slate-200">
                <div class="py-10 text-center group hover:bg-slate-50 transition-colors">
                    <div class="text-4xl font-black text-slate-900 mb-1 group-hover:text-amber-600 transition-colors">3+</div>
                    <div class="text-xs text-slate-500 uppercase tracking-[0.2em] font-bold">Years Active</div>
                </div>
                <div class="py-10 text-center group hover:bg-slate-50 transition-colors">
                    <div class="text-4xl font-black text-slate-900 mb-1 group-hover:text-amber-600 transition-colors">15+</div>
                    <div class="text-xs text-slate-500 uppercase tracking-[0.2em] font-bold">Deployments</div>
                </div>
                <div class="py-10 text-center group hover:bg-slate-50 transition-colors">
                    <div class="text-4xl font-black text-slate-900 mb-1 group-hover:text-amber-600 transition-colors">100%</div>
                    <div class="text-xs text-slate-500 uppercase tracking-[0.2em] font-bold">Success Rate</div>
                </div>
                <div class="py-10 text-center group hover:bg-slate-50 transition-colors">
                    <div class="text-4xl font-black text-slate-900 mb-1 group-hover:text-amber-600 transition-colors">24/7</div>
                    <div class="text-xs text-slate-500 uppercase tracking-[0.2em] font-bold">Monitoring</div>
                </div>
            </div>
        </div>
    </section>

    <!-- THE ARSENAL (Marquee) -->
    <section class="py-8 border-b border-slate-200 bg-slate-50 overflow-hidden relative z-20">
        <div class="max-w-7xl mx-auto px-4 mb-4 text-center">
             <h3 class="text-amber-600 text-xs font-bold tracking-[0.3em] uppercase opacity-70">Detterminant For Success</h3>
        </div>
        <div class="relative w-full overflow-hidden">
            <div class="flex gap-16 items-center animate-scroll whitespace-nowrap py-4">
                <span class="text-lg font-bold text-slate-400 font-mono flex items-center gap-2 hover:text-slate-900 transition-colors cursor-default"><i data-lucide="terminal" class="w-5 h-5 text-amber-500"></i> Kali Linux</span>
                <span class="text-lg font-bold text-slate-400 font-mono flex items-center gap-2 hover:text-slate-900 transition-colors cursor-default"><i data-lucide="code" class="w-5 h-5 text-amber-500"></i> Python</span>
                <span class="text-lg font-bold text-slate-400 font-mono flex items-center gap-2 hover:text-slate-900 transition-colors cursor-default"><i data-lucide="shield-alert" class="w-5 h-5 text-amber-500"></i> Metasploit</span>
                <span class="text-lg font-bold text-slate-400 font-mono flex items-center gap-2 hover:text-slate-900 transition-colors cursor-default"><i data-lucide="network" class="w-5 h-5 text-amber-500"></i> Wireshark</span>
                <span class="text-lg font-bold text-slate-400 font-mono flex items-center gap-2 hover:text-slate-900 transition-colors cursor-default"><i data-lucide="file-spreadsheet" class="w-5 h-5 text-amber-500"></i> Excel (Adv)</span>
                
                <span class="text-lg font-bold text-slate-400 font-mono flex items-center gap-2 hover:text-slate-900 transition-colors cursor-default"><i data-lucide="terminal" class="w-5 h-5 text-amber-500"></i> Kali Linux</span>
                <span class="text-lg font-bold text-slate-400 font-mono flex items-center gap-2 hover:text-slate-900 transition-colors cursor-default"><i data-lucide="code" class="w-5 h-5 text-amber-500"></i> Python</span>
                <span class="text-lg font-bold text-slate-400 font-mono flex items-center gap-2 hover:text-slate-900 transition-colors cursor-default"><i data-lucide="shield-alert" class="w-5 h-5 text-amber-500"></i> Metasploit</span>
                <span class="text-lg font-bold text-slate-400 font-mono flex items-center gap-2 hover:text-slate-900 transition-colors cursor-default"><i data-lucide="network" class="w-5 h-5 text-amber-500"></i> Wireshark</span>
                <span class="text-lg font-bold text-slate-400 font-mono flex items-center gap-2 hover:text-slate-900 transition-colors cursor-default"><i data-lucide="file-spreadsheet" class="w-5 h-5 text-amber-500"></i> Excel (Adv)</span>
            </div>
        </div>
    </section>

    <!-- Expertise Section -->
    <section id="expertise" class="py-32 relative bg-white">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="mb-24">
                <h2 class="text-5xl md:text-7xl font-black text-slate-900 mb-6 uppercase tracking-tight hacker-text" data-value="CORE_MODULES">MODULES</h2>
                <div class="h-1 w-32 bg-gradient-to-r from-amber-500 to-transparent"></div>
            </div>

            <div class="grid md:grid-cols-3 gap-8">
                <!-- Card 1 -->
                <div class="scanline-card glass p-8 border-l-4 border-amber-500 hover:bg-slate-50 transition-all group shadow-lg">
                    <div class="mb-8 relative">
                        <div class="w-16 h-16 bg-amber-50 flex items-center justify-center border border-amber-500 group-hover:rotate-45 transition-transform duration-500">
                            <i data-lucide="shield" class="w-8 h-8 text-amber-600 group-hover:-rotate-45 transition-transform duration-500"></i>
                        </div>
                    </div>
                    <h3 class="text-2xl font-bold text-slate-900 mb-4 uppercase tracking-wide">Cyber Security</h3>
                    <p class="text-slate-500 leading-relaxed mb-8">
                        Offensive and defensive operations. Vulnerability assessment, penetration testing, and infrastructure hardening.
                    </p>
                    <ul class="space-y-2 text-sm text-slate-700 font-mono">
                        <li class="flex items-center gap-2"><span class="text-amber-500">></span> Pen-Testing</li>
                        <li class="flex items-center gap-2"><span class="text-amber-500">></span> Risk Analysis</li>
                    </ul>
                </div>

                <!-- Card 2 -->
                <div class="scanline-card glass p-8 border-l-4 border-slate-900 hover:bg-slate-50 transition-all group shadow-lg">
                    <div class="mb-8 relative">
                        <div class="w-16 h-16 bg-slate-100 flex items-center justify-center border border-slate-900 group-hover:rotate-45 transition-transform duration-500">
                            <i data-lucide="activity" class="w-8 h-8 text-slate-900 group-hover:-rotate-45 transition-transform duration-500"></i>
                        </div>
                    </div>
                    <h3 class="text-2xl font-bold text-slate-900 mb-4 uppercase tracking-wide">Accountancy</h3>
                    <p class="text-slate-500 leading-relaxed mb-8">
                        Financial precision meets regulatory compliance. Auditing, bookkeeping, and strategic financial planning.
                    </p>
                    <ul class="space-y-2 text-sm text-slate-700 font-mono">
                        <li class="flex items-center gap-2"><span class="text-slate-900">></span> Auditing</li>
                        <li class="flex items-center gap-2"><span class="text-slate-900">></span> Forecasting</li>
                    </ul>
                </div>

                <!-- Card 3 -->
                <div class="scanline-card glass p-8 border-l-4 border-amber-500 hover:bg-slate-50 transition-all group shadow-lg">
                    <div class="mb-8 relative">
                        <div class="w-16 h-16 bg-amber-50 flex items-center justify-center border border-amber-500 group-hover:rotate-45 transition-transform duration-500">
                            <i data-lucide="cpu" class="w-8 h-8 text-amber-600 group-hover:-rotate-45 transition-transform duration-500"></i>
                        </div>
                    </div>
                    <h3 class="text-2xl font-bold text-slate-900 mb-4 uppercase tracking-wide">Freelancing</h3>
                    <p class="text-slate-500 leading-relaxed mb-8">
                        Agile engagement models for startups and enterprises. Rapid deployment of skills where you need them most.
                    </p>
                    <ul class="space-y-2 text-sm text-slate-700 font-mono">
                        <li class="flex items-center gap-2"><span class="text-amber-500">></span> Remote Ops</li>
                        <li class="flex items-center gap-2"><span class="text-amber-500">></span> Contract</li>
                    </ul>
                </div>
            </div>
        </div>
    </section>

    <!-- NEW SECTION: WHY CHOOSE ME (Value Proposition) -->
    <section class="py-32 bg-slate-50 relative">
        <div class="max-w-7xl mx-auto px-4">
            <h2 class="text-4xl md:text-5xl font-black text-slate-900 mb-20 uppercase tracking-tight hacker-text text-center" data-value="VALUE_PROPOSITION">VALUE_PROPOSITION</h2>
            
            <div class="grid md:grid-cols-2 gap-16 items-center">
                <div class="space-y-12">
                    <div class="flex gap-6">
                        <div class="w-12 h-12 bg-white rounded-lg flex items-center justify-center border border-slate-200 shadow-md flex-shrink-0">
                            <i data-lucide="target" class="w-6 h-6 text-amber-600"></i>
                        </div>
                        <div>
                            <h3 class="text-xl font-bold text-slate-900 mb-2">Dual-Layer Protection</h3>
                            <p class="text-slate-500 leading-relaxed">Most consultants focus on either money or security. I handle both. I ensure your financial data isn't just accurate, but impenetrable.</p>
                        </div>
                    </div>
                    <div class="flex gap-6">
                        <div class="w-12 h-12 bg-white rounded-lg flex items-center justify-center border border-slate-200 shadow-md flex-shrink-0">
                            <i data-lucide="clock" class="w-6 h-6 text-amber-600"></i>
                        </div>
                        <div>
                            <h3 class="text-xl font-bold text-slate-900 mb-2">Rapid Mobilization</h3>
                            <p class="text-slate-500 leading-relaxed">Time is money and risk. I deploy faster than traditional agencies, with zero onboarding friction for standard financial or security audits.</p>
                        </div>
                    </div>
                    <div class="flex gap-6">
                        <div class="w-12 h-12 bg-white rounded-lg flex items-center justify-center border border-slate-200 shadow-md flex-shrink-0">
                            <i data-lucide="award" class="w-6 h-6 text-amber-600"></i>
                        </div>
                        <div>
                            <h3 class="text-xl font-bold text-slate-900 mb-2">Certified Excellence</h3>
                            <p class="text-slate-500 leading-relaxed">Backed by globally recognized certifications (CISSP, CPA), delivering institutional-grade quality to every client.</p>
                        </div>
                    </div>
                </div>
                
                <div class="relative">
                    <div class="absolute inset-0 bg-gradient-to-r from-amber-500 to-slate-900 rounded-2xl transform rotate-3"></div>
                    <div class="bg-white border border-slate-200 rounded-2xl p-8 relative shadow-2xl">
                        <h4 class="font-mono text-xs text-amber-600 uppercase mb-4 tracking-widest">Client Metrics</h4>
                        <div class="space-y-6">
                            <div>
                                <div class="flex justify-between text-sm font-bold text-slate-900 mb-2">
                                    <span>Vulnerability Reduction</span>
                                    <span>98%</span>
                                </div>
                                <div class="w-full bg-slate-100 rounded-full h-2">
                                    <div class="bg-amber-500 h-2 rounded-full" style="width: 98%"></div>
                                </div>
                            </div>
                            <div>
                                <div class="flex justify-between text-sm font-bold text-slate-900 mb-2">
                                    <span>Audit Accuracy</span>
                                    <span>100%</span>
                                </div>
                                <div class="w-full bg-slate-100 rounded-full h-2">
                                    <div class="bg-slate-900 h-2 rounded-full" style="width: 100%"></div>
                                </div>
                            </div>
                            <div>
                                <div class="flex justify-between text-sm font-bold text-slate-900 mb-2">
                                    <span>Client Retention</span>
                                    <span>95%</span>
                                </div>
                                <div class="w-full bg-slate-100 rounded-full h-2">
                                    <div class="bg-slate-400 h-2 rounded-full" style="width: 95%"></div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- MISSION PROTOCOL (Process) -->
    <section class="py-32 relative bg-white">
        <div class="absolute inset-0 bg-[radial-gradient(#cbd5e1_1px,transparent_1px)] [background-size:20px_20px] opacity-50"></div>
        <div class="max-w-7xl mx-auto px-4 relative z-10">
            <h2 class="text-4xl md:text-5xl font-black text-slate-900 mb-20 uppercase tracking-tight hacker-text text-center" data-value="MISSION_PROTOCOL">MISSION_PROTOCOL</h2>
            
            <div class="grid md:grid-cols-4 gap-8">
                <!-- Step 1 -->
                <div class="group text-center relative">
                    <div class="absolute top-8 right-[-50%] w-full h-[1px] bg-slate-300 hidden md:block"></div>
                    <div class="w-20 h-20 bg-white border border-slate-300 rounded-full flex items-center justify-center mx-auto mb-6 relative z-10 shadow-sm group-hover:border-amber-500 transition-colors">
                        <i data-lucide="search" class="w-8 h-8 text-slate-400 group-hover:text-amber-600 transition-colors"></i>
                    </div>
                    <h3 class="text-xl font-bold text-slate-900 mb-3">1. Reconnaissance</h3>
                    <p class="text-sm text-slate-500 font-mono">Deep dive analysis of your security posture or financial data.</p>
                </div>
                <!-- Steps 2, 3, 4 would follow similar pattern with different icons -->
                <div class="group text-center relative">
                     <div class="absolute top-8 right-[-50%] w-full h-[1px] bg-slate-300 hidden md:block"></div>
                    <div class="w-20 h-20 bg-white border border-slate-300 rounded-full flex items-center justify-center mx-auto mb-6 relative z-10 shadow-sm group-hover:border-amber-500 transition-colors">
                        <i data-lucide="crosshair" class="w-8 h-8 text-slate-400 group-hover:text-amber-600 transition-colors"></i>
                    </div>
                    <h3 class="text-xl font-bold text-slate-900 mb-3">2. Strategy</h3>
                    <p class="text-sm text-slate-500 font-mono">Formulating a tactical plan to mitigate risks.</p>
                </div>
                <div class="group text-center relative">
                     <div class="absolute top-8 right-[-50%] w-full h-[1px] bg-slate-300 hidden md:block"></div>
                    <div class="w-20 h-20 bg-white border border-slate-300 rounded-full flex items-center justify-center mx-auto mb-6 relative z-10 shadow-sm group-hover:border-amber-500 transition-colors">
                        <i data-lucide="zap" class="w-8 h-8 text-slate-400 group-hover:text-amber-600 transition-colors"></i>
                    </div>
                    <h3 class="text-xl font-bold text-slate-900 mb-3">3. Deployment</h3>
                    <p class="text-sm text-slate-500 font-mono">Executing the audit or financial restructuring.</p>
                </div>
                <div class="group text-center relative">
                    <div class="w-20 h-20 bg-white border border-slate-300 rounded-full flex items-center justify-center mx-auto mb-6 relative z-10 shadow-sm group-hover:border-amber-500 transition-colors">
                        <i data-lucide="check-circle" class="w-8 h-8 text-slate-400 group-hover:text-amber-600 transition-colors"></i>
                    </div>
                    <h3 class="text-xl font-bold text-slate-900 mb-3">4. Debrief</h3>
                    <p class="text-sm text-slate-500 font-mono">Comprehensive reporting and future-proofing.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- NEW SECTION: TESTIMONIALS -->
    <section class="py-32 bg-slate-50 relative border-t border-slate-200">
        <div class="max-w-7xl mx-auto px-4">
            <h2 class="text-4xl md:text-5xl font-black text-slate-900 mb-16 uppercase tracking-tight hacker-text text-center" data-value="CLIENT_FEEDBACK">CLIENT_FEEDBACK</h2>
            
            <div class="grid md:grid-cols-3 gap-8">
                <!-- Review 1 -->
                <div class="bg-white p-8 rounded-xl shadow-sm border border-slate-200 relative group hover:-translate-y-2 transition-transform">
                    <i data-lucide="quote" class="w-10 h-10 text-slate-100 absolute top-4 right-4"></i>
                    <div class="flex gap-1 mb-4">
                        <i data-lucide="star" class="w-4 h-4 text-amber-500 fill-amber-500"></i>
                        <i data-lucide="star" class="w-4 h-4 text-amber-500 fill-amber-500"></i>
                        <i data-lucide="star" class="w-4 h-4 text-amber-500 fill-amber-500"></i>
                        <i data-lucide="star" class="w-4 h-4 text-amber-500 fill-amber-500"></i>
                        <i data-lucide="star" class="w-4 h-4 text-amber-500 fill-amber-500"></i>
                    </div>
                    <p class="text-slate-600 mb-6 italic">"Tauqeer found a critical vulnerability in our API that three previous security firms missed. His financial insight also helped streamline our billing logic."</p>
                    <div class="flex items-center gap-3">
                        <div class="w-10 h-10 bg-slate-900 rounded-full flex items-center justify-center text-white font-bold text-xs">JD</div>
                        <div>
                            <h4 class="text-sm font-bold text-slate-900">John Doe</h4>
                            <p class="text-xs text-slate-500">CTO, FinTech Global</p>
                        </div>
                    </div>
                </div>

                <!-- Review 2 -->
                <div class="bg-white p-8 rounded-xl shadow-sm border border-slate-200 relative group hover:-translate-y-2 transition-transform">
                    <i data-lucide="quote" class="w-10 h-10 text-slate-100 absolute top-4 right-4"></i>
                    <div class="flex gap-1 mb-4">
                        <i data-lucide="star" class="w-4 h-4 text-amber-500 fill-amber-500"></i>
                        <i data-lucide="star" class="w-4 h-4 text-amber-500 fill-amber-500"></i>
                        <i data-lucide="star" class="w-4 h-4 text-amber-500 fill-amber-500"></i>
                        <i data-lucide="star" class="w-4 h-4 text-amber-500 fill-amber-500"></i>
                        <i data-lucide="star" class="w-4 h-4 text-amber-500 fill-amber-500"></i>
                    </div>
                    <p class="text-slate-600 mb-6 italic">"Extremely professional and detail-oriented. He audited our books for the fiscal year and cleaned up a mess of discrepancies in just a week."</p>
                    <div class="flex items-center gap-3">
                        <div class="w-10 h-10 bg-slate-900 rounded-full flex items-center justify-center text-white font-bold text-xs">AS</div>
                        <div>
                            <h4 class="text-sm font-bold text-slate-900">Sarah A.</h4>
                            <p class="text-xs text-slate-500">Founder, Logistics Co.</p>
                        </div>
                    </div>
                </div>

                <!-- Review 3 -->
                <div class="bg-white p-8 rounded-xl shadow-sm border border-slate-200 relative group hover:-translate-y-2 transition-transform">
                    <i data-lucide="quote" class="w-10 h-10 text-slate-100 absolute top-4 right-4"></i>
                    <div class="flex gap-1 mb-4">
                        <i data-lucide="star" class="w-4 h-4 text-amber-500 fill-amber-500"></i>
                        <i data-lucide="star" class="w-4 h-4 text-amber-500 fill-amber-500"></i>
                        <i data-lucide="star" class="w-4 h-4 text-amber-500 fill-amber-500"></i>
                        <i data-lucide="star" class="w-4 h-4 text-amber-500 fill-amber-500"></i>
                        <i data-lucide="star" class="w-4 h-4 text-amber-500 fill-amber-500"></i>
                    </div>
                    <p class="text-slate-600 mb-6 italic">"The best freelancer I've hired on Upwork. Communicates clearly, delivers on time, and the quality of work is institutional grade."</p>
                    <div class="flex items-center gap-3">
                        <div class="w-10 h-10 bg-slate-900 rounded-full flex items-center justify-center text-white font-bold text-xs">MK</div>
                        <div>
                            <h4 class="text-sm font-bold text-slate-900">Mike K.</h4>
                            <p class="text-xs text-slate-500">Director, E-Comm Inc.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Terminal Bio & Holographic Card -->
    <section id="about" class="py-32 bg-white">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="grid lg:grid-cols-2 gap-20 items-center">
                <!-- Left: Holographic ID -->
                <div>
                     <h2 class="text-5xl font-black text-slate-900 mb-10 hacker-text" data-value="OPERATIVE_ID">OPERATIVE_ID</h2>
                     
                     <!-- HOLOGRAPHIC CARD (White) -->
                     <div class="holo-card rounded-xl p-6 mb-10 tilt-card" data-tilt>
                         <div class="scan-line"></div>
                         <div class="flex items-start justify-between mb-6">
                             <div class="flex gap-4">
                                 <div class="w-20 h-20 bg-slate-50 border border-slate-200 rounded flex items-center justify-center">
                                     <i data-lucide="user" class="w-10 h-10 text-amber-500"></i>
                                 </div>
                                 <div>
                                     <h3 class="text-xl font-bold text-slate-900 tracking-widest">TAUQEER M.</h3>
                                     <p class="text-xs text-amber-600 font-mono mb-1">LVL 5 SECURITY CLEARANCE</p>
                                     <div class="flex gap-2">
                                         <span class="px-2 py-0.5 bg-green-100 text-green-700 text-[10px] font-bold border border-green-200 rounded">ACTIVE</span>
                                     </div>
                                 </div>
                             </div>
                             <i data-lucide="qr-code" class="w-12 h-12 text-slate-300"></i>
                         </div>
                         
                         <div class="grid grid-cols-2 gap-4 text-xs font-mono text-slate-500 border-t border-slate-100 pt-4">
                             <div>
                                 <span class="block text-slate-400 mb-1">SPECIALIZATION</span>
                                 <span class="text-slate-900">SecOps / Audit</span>
                             </div>
                             <div>
                                 <span class="block text-slate-400 mb-1">BASE OF OPS</span>
                                 <span class="text-slate-900">Remote / Worldwide</span>
                             </div>
                         </div>
                     </div>
                </div>

                <!-- Right: Interactive Terminal (Light Mode) -->
                <div class="bg-white border border-slate-300 rounded-lg shadow-2xl relative overflow-hidden group">
                    <div class="bg-slate-100 rounded-t px-4 py-2 flex items-center justify-between border-b border-slate-200">
                        <div class="flex items-center gap-2">
                            <div class="w-3 h-3 rounded-full bg-red-400"></div>
                            <div class="w-3 h-3 rounded-full bg-amber-400"></div>
                            <div class="w-3 h-3 rounded-full bg-green-400"></div>
                        </div>
                        <div class="text-xs text-slate-500 font-mono">guest@tauqeer-shell:~</div>
                    </div>
                    <div class="p-6 font-mono text-sm min-h-[300px] text-slate-700 relative" onclick="document.getElementById('term-input').focus()">
                        <div id="term-output">
                            <div class="mb-4">
                                <span class="text-green-600">➜</span> <span class="text-blue-600">~</span> ./welcome.sh
                            </div>
                            <div class="mb-4 text-slate-900">
                                Welcome to Tauqeer's Interface.<br>
                                Type <span class="text-amber-600 font-bold">'help'</span> to see available commands.
                            </div>
                        </div>
                        <div class="flex items-center">
                            <span class="text-green-600 mr-2">➜</span> 
                            <span class="text-blue-600 mr-2">~</span>
                            <input type="text" id="term-input" class="bg-transparent border-none outline-none text-slate-900 w-full" autocomplete="off" spellcheck="false">
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- NEW SECTION: FAQ -->
    <section class="py-32 bg-slate-50 relative border-t border-slate-200">
        <div class="max-w-3xl mx-auto px-4">
            <h2 class="text-4xl font-black text-slate-900 mb-12 text-center uppercase tracking-tight">Frequently Asked Questions</h2>
            
            <div class="space-y-4">
                <div class="accordion-item bg-white rounded-lg px-6 shadow-sm">
                    <div class="accordion-header" onclick="this.parentElement.classList.toggle('active')">
                        <span class="font-bold text-slate-900">What services do you offer specifically?</span>
                        <i data-lucide="chevron-down" class="accordion-icon w-5 h-5 text-slate-400"></i>
                    </div>
                    <div class="accordion-content text-slate-600 text-sm">
                        I specialize in Network Security Audits, Web Application Penetration Testing, Financial Auditing, Bookkeeping (QuickBooks/Xero), and Financial Risk Analysis.
                    </div>
                </div>
                
                <div class="accordion-item bg-white rounded-lg px-6 shadow-sm">
                    <div class="accordion-header" onclick="this.parentElement.classList.toggle('active')">
                        <span class="font-bold text-slate-900">Do you work with international clients?</span>
                        <i data-lucide="chevron-down" class="accordion-icon w-5 h-5 text-slate-400"></i>
                    </div>
                    <div class="accordion-content text-slate-600 text-sm">
                        Yes, 100%. I have worked with clients from the US, UK, Canada, and UAE. All my operations are optimized for remote delivery.
                    </div>
                </div>

                <div class="accordion-item bg-white rounded-lg px-6 shadow-sm">
                    <div class="accordion-header" onclick="this.parentElement.classList.toggle('active')">
                        <span class="font-bold text-slate-900">How do you ensure data confidentiality?</span>
                        <i data-lucide="chevron-down" class="accordion-icon w-5 h-5 text-slate-400"></i>
                    </div>
                    <div class="accordion-content text-slate-600 text-sm">
                        I adhere to strict NDA protocols. All data is handled on encrypted devices, and I use secure channels for all communications and file transfers.
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- CERTIFICATIONS -->
    <section class="py-32 bg-white relative">
        <div class="max-w-7xl mx-auto px-4">
            <h2 class="text-4xl md:text-5xl font-black text-slate-900 mb-16 uppercase tracking-tight hacker-text text-center" data-value="CREDENTIALS_VERIFIED">CREDENTIALS_VERIFIED</h2>
            <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
                <!-- Cert 1 -->
                <div class="bg-slate-50 border border-slate-200 p-6 rounded-lg text-center hover:shadow-lg transition-all group hover:border-amber-500">
                    <div class="w-16 h-16 bg-white rounded-full flex items-center justify-center mx-auto mb-4 border border-slate-300 group-hover:border-amber-500">
                        <i data-lucide="award" class="w-8 h-8 text-slate-400 group-hover:text-amber-600"></i>
                    </div>
                    <h4 class="font-bold text-slate-900">CISSP</h4>
                    <p class="text-xs text-slate-500 uppercase tracking-wide">Certified Information Systems Security Professional</p>
                </div>
                <!-- Cert 2 -->
                <div class="bg-slate-50 border border-slate-200 p-6 rounded-lg text-center hover:shadow-lg transition-all group hover:border-slate-900">
                    <div class="w-16 h-16 bg-white rounded-full flex items-center justify-center mx-auto mb-4 border border-slate-300 group-hover:border-slate-900">
                        <i data-lucide="scroll" class="w-8 h-8 text-slate-400 group-hover:text-slate-900"></i>
                    </div>
                    <h4 class="font-bold text-slate-900">CPA</h4>
                    <p class="text-xs text-slate-500 uppercase tracking-wide">Certified Public Accountant</p>
                </div>
                <!-- Cert 3 -->
                <div class="bg-slate-50 border border-slate-200 p-6 rounded-lg text-center hover:shadow-lg transition-all group hover:border-amber-500">
                    <div class="w-16 h-16 bg-white rounded-full flex items-center justify-center mx-auto mb-4 border border-slate-300 group-hover:border-amber-500">
                        <i data-lucide="shield-check" class="w-8 h-8 text-slate-400 group-hover:text-amber-600"></i>
                    </div>
                    <h4 class="font-bold text-slate-900">CEH</h4>
                    <p class="text-xs text-slate-500 uppercase tracking-wide">Certified Ethical Hacker</p>
                </div>
                <!-- Cert 4 -->
                <div class="bg-slate-50 border border-slate-200 p-6 rounded-lg text-center hover:shadow-lg transition-all group hover:border-slate-900">
                    <div class="w-16 h-16 bg-white rounded-full flex items-center justify-center mx-auto mb-4 border border-slate-300 group-hover:border-slate-900">
                        <i data-lucide="book-open" class="w-8 h-8 text-slate-400 group-hover:text-slate-900"></i>
                    </div>
                    <h4 class="font-bold text-slate-900">ACCA</h4>
                    <p class="text-xs text-slate-500 uppercase tracking-wide">Association of Chartered Certified Accountants</p>
                </div>
            </div>
        </div>
    </section>

    <!-- NEW SECTION: LATEST INSIGHTS (Blog) -->
    <section class="py-32 bg-slate-50 relative border-t border-slate-200">
        <div class="max-w-7xl mx-auto px-4">
            <h2 class="text-4xl md:text-5xl font-black text-slate-900 mb-16 uppercase tracking-tight hacker-text text-center" data-value="LATEST_INTEL">LATEST_INTEL</h2>
            <div class="grid md:grid-cols-3 gap-8">
                <!-- Article 1 -->
                <div class="group cursor-pointer">
                    <div class="h-48 bg-white rounded-lg mb-4 overflow-hidden relative border border-slate-200">
                        <div class="absolute inset-0 bg-slate-900/5 group-hover:bg-transparent transition-colors"></div>
                        <!-- Placeholder visual -->
                        <div class="w-full h-full flex items-center justify-center text-slate-300"><i data-lucide="file-text" class="w-12 h-12"></i></div>
                    </div>
                    <div class="flex items-center gap-2 text-xs text-amber-600 font-bold uppercase mb-2">
                        <span>Security</span> • <span>Oct 24, 2024</span>
                    </div>
                    <h3 class="text-xl font-bold text-slate-900 mb-2 group-hover:text-amber-600 transition-colors">Zero Trust Architecture in Fintech</h3>
                    <p class="text-slate-500 text-sm leading-relaxed">Why the perimeter defense model is failing and how modern financial institutions are adapting.</p>
                </div>
                <!-- Article 2 -->
                <div class="group cursor-pointer">
                    <div class="h-48 bg-white rounded-lg mb-4 overflow-hidden relative border border-slate-200">
                        <div class="absolute inset-0 bg-slate-900/5 group-hover:bg-transparent transition-colors"></div>
                        <div class="w-full h-full flex items-center justify-center text-slate-300"><i data-lucide="trending-up" class="w-12 h-12"></i></div>
                    </div>
                    <div class="flex items-center gap-2 text-xs text-slate-900 font-bold uppercase mb-2">
                        <span>Finance</span> • <span>2025</span>
                    </div>
                    <h3 class="text-xl font-bold text-slate-900 mb-2 group-hover:text-amber-600 transition-colors">Forensic Accounting 101</h3>
                    <p class="text-slate-500 text-sm leading-relaxed">Detecting fraud through data patterns: A guide to anomalies in the ledger.</p>
                </div>
                <!-- Article 3 -->
                <div class="group cursor-pointer">
                    <div class="h-48 bg-white rounded-lg mb-4 overflow-hidden relative border border-slate-200">
                        <div class="absolute inset-0 bg-slate-900/5 group-hover:bg-transparent transition-colors"></div>
                        <div class="w-full h-full flex items-center justify-center text-slate-300"><i data-lucide="terminal" class="w-12 h-12"></i></div>
                    </div>
                    <div class="flex items-center gap-2 text-xs text-amber-600 font-bold uppercase mb-2">
                        <span>Tech</span> • <span>2025</span>
                    </div>
                    <h3 class="text-xl font-bold text-slate-900 mb-2 group-hover:text-amber-600 transition-colors">Automating Audits with Python</h3>
                    <p class="text-slate-500 text-sm leading-relaxed">Scripts that save hours of manual reconciliation work for auditors.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- FOOTER & SOCIALS -->
    <footer class="bg-white border-t border-slate-200 pt-20 pb-10">
        <div class="max-w-7xl mx-auto px-4">
            <div class="grid md:grid-cols-4 gap-12 mb-16">
                <div class="col-span-1">
                    <div class="flex items-center gap-4 mb-6">
                        <div class="w-10 h-10 border border-amber-500 flex items-center justify-center text-amber-600 font-bold">Tauqeer Mustafa</div>
                        <span class="text-slate-900 font-bold tracking-widest uppercase">Tauqeer Mustafa</span>
                    </div>
                    <p class="text-slate-500 text-sm">Securing digital assets and balancing financial ledgers.</p>
                </div>
                
                <div>
                    <h4 class="text-slate-900 font-bold uppercase tracking-wider mb-6">Connect</h4>
                    <div class="flex gap-4 flex-wrap">
                        <!-- LinkedIn -->
                        <a href="https://linkedin.com/in/tauqeermustafaa" target="_blank" class="w-10 h-10 rounded-full bg-slate-50 flex items-center justify-center transition-all hover:scale-110 border border-slate-200 hover:border-blue-600 hover:text-blue-600 group hover:shadow-lg"><i data-lucide="linkedin" class="w-5 h-5 text-slate-400 group-hover:text-blue-600"></i></a>
                        <!-- GitHub -->
                        <a href="https://github.com/tauqeermustafa" target="_blank" class="w-10 h-10 rounded-full bg-slate-50 flex items-center justify-center transition-all hover:scale-110 border border-slate-200 hover:border-black hover:text-black group hover:shadow-lg"><i data-lucide="github" class="w-5 h-5 text-slate-400 group-hover:text-black"></i></a>
                        <!-- Twitter -->
                        <a href="https://twitter.com/tauqeermustafaa" target="_blank" class="w-10 h-10 rounded-full bg-slate-50 flex items-center justify-center transition-all hover:scale-110 border border-slate-200 hover:border-sky-500 hover:text-sky-500 group hover:shadow-lg"><i data-lucide="twitter" class="w-5 h-5 text-slate-400 group-hover:text-sky-500"></i></a>
                        <!-- Instagram -->
                        <a href="https://instagram.com/tauqeermustafaa" target="_blank" class="w-10 h-10 rounded-full bg-slate-50 flex items-center justify-center transition-all hover:scale-110 border border-slate-200 hover:border-pink-600 hover:text-pink-600 group hover:shadow-lg"><i data-lucide="instagram" class="w-5 h-5 text-slate-400 group-hover:text-pink-600"></i></a>
                        <!-- Facebook (New) -->
                        <a href="https://facebook.com/tauqeermustafaa" target="_blank" class="w-10 h-10 rounded-full bg-slate-50 flex items-center justify-center transition-all hover:scale-110 border border-slate-200 hover:border-blue-800 hover:text-blue-800 group hover:shadow-lg"><i data-lucide="facebook" class="w-5 h-5 text-slate-400 group-hover:text-blue-800"></i></a>
                        <!-- WhatsApp (Fixed: Correct icon & link style) -->
                        <a href="https://wa.me/+923404941658" target="_blank" class="w-10 h-10 rounded-full bg-slate-50 flex items-center justify-center transition-all hover:scale-110 border border-slate-200 hover:border-green-500 hover:text-green-500 group hover:shadow-lg"><i data-lucide="message-circle" class="w-5 h-5 text-slate-400 group-hover:text-green-500"></i></a>
                    </div>
                </div>
            </div>
            
            <div class="border-t border-slate-200 pt-8 flex justify-between items-center text-xs text-slate-400">
                <div>&copy; 2025 Tauqeer Mustafa. All rights reserved.</div>

            </div>
        </div>
    </footer>

    <!-- AI CHAT -->
    <!-- (Existing AI Chat code remains the same) -->
    <div class="ai-modal" id="ai-modal">
        <div class="bg-slate-50 p-4 border-b border-slate-200 flex justify-between items-center rounded-t-xl">
            <div class="flex items-center gap-2">
                <div class="w-2 h-2 rounded-full bg-green-500 animate-pulse"></div>
                <span class="text-slate-900 font-bold font-mono text-sm">ASSISTANT_V1.0</span>
            </div>
            <button id="close-chat" class="text-slate-400 hover:text-slate-900"><i data-lucide="x" class="w-4 h-4"></i></button>
        </div>
        <div class="chat-messages p-4 overflow-y-auto flex-grow" id="chat-messages">
            <div class="bg-slate-100 p-3 rounded-lg text-sm text-slate-700 max-w-[85%] self-start">
                I am Tauqeer's assistant. Ask about his skills or projects.
            </div>
        </div>
        <div class="p-4 border-t border-slate-200 bg-white rounded-b-xl">
            <div class="flex gap-2">
                <input type="text" id="chat-input" placeholder="Type command..." class="flex-grow bg-slate-50 border border-slate-200 rounded px-3 py-2 text-slate-900 text-sm focus:outline-none focus:border-amber-500">
                <button id="send-chat" class="bg-amber-500 text-white px-3 py-2 rounded font-bold hover:bg-amber-600 transition-colors">
                    <i data-lucide="send" class="w-4 h-4"></i>
                </button>
            </div>
        </div>
    </div>

    <button class="fixed bottom-8 right-8 w-14 h-14 bg-amber-500 text-white rounded-full shadow-xl flex items-center justify-center hover:scale-110 transition-transform z-50" id="ai-fab">
        <i data-lucide="bot" class="w-7 h-7"></i>
    </button>

    <script>
        lucide.createIcons();
        const apiKey = "";

        // --- CUSTOM CURSOR ---
        const cursorDot = document.querySelector('.cursor-dot');
        const cursorOutline = document.querySelector('.cursor-outline');
        window.addEventListener('mousemove', (e) => {
            const posX = e.clientX;
            const posY = e.clientY;
            cursorDot.style.left = `${posX}px`; cursorDot.style.top = `${posY}px`;
            cursorOutline.animate({ left: `${posX}px`, top: `${posY}px` }, { duration: 500, fill: "forwards" });
        });

        // --- HACKER TEXT ---
        const letters = "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789";
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                    let iterations = 0;
                    const target = entry.target;
                    const originalText = target.dataset.value;
                    const interval = setInterval(() => {
                        target.innerText = originalText.split("").map((letter, index) => {
                            if(index < iterations) return originalText[index];
                            return letters[Math.floor(Math.random() * 36)];
                        }).join("");
                        if(iterations >= originalText.length) clearInterval(interval);
                        iterations += 1/3;
                    }, 30);
                    observer.unobserve(target);
                }
            });
        }, { threshold: 0.5 });
        document.querySelectorAll('.hacker-text').forEach(el => observer.observe(el));

        // --- CANVAS (Light Mode) ---
        const canvas = document.getElementById('canvas');
        const ctx = canvas.getContext('2d');
        let width, height, particles = [], mouse = {x: null, y: null, radius: 250};
        window.addEventListener('mousemove', (e) => { mouse.x = e.x; mouse.y = e.y; });
        function resize() { width = canvas.width = window.innerWidth; height = canvas.height = window.innerHeight; initParticles(); }
        window.addEventListener('resize', resize);
        class Particle {
            constructor() {
                this.x = Math.random() * width; this.y = Math.random() * height;
                this.vx = (Math.random() - 0.5); this.vy = (Math.random() - 0.5);
                this.size = Math.random() * 2 + 1;
                
                // UPDATED COLORS: Black, Gold, Neon Yellow, Blue
                const colors = ['#0f172a', '#d97706', '#facc15', '#2563eb'];
                this.color = colors[Math.floor(Math.random() * colors.length)];
            }
            update() {
                this.x += this.vx; this.y += this.vy;
                if(mouse.x != null) {
                    let dx = mouse.x - this.x; let dy = mouse.y - this.y;
                    let distance = Math.sqrt(dx*dx + dy*dy);
                    if(distance < mouse.radius) {
                        const forceDirectionX = dx / distance; const forceDirectionY = dy / distance;
                        const force = (mouse.radius - distance) / mouse.radius;
                        this.x -= forceDirectionX * force * 5; this.y -= forceDirectionY * force * 5;
                    }
                }
                if(this.x < 0 || this.x > width) this.vx *= -1;
                if(this.y < 0 || this.y > height) this.vy *= -1;
            }
            draw() { ctx.beginPath(); ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2); ctx.fillStyle = this.color; ctx.fill(); }
        }
        
        // INCREASED DENSITY: Changed divisor from 9000 to 4000 (More particles)
        function initParticles() { particles = []; for(let i=0; i<(width*height)/4000; i++) particles.push(new Particle()); }
        
        function animate() {
            ctx.clearRect(0, 0, width, height);
            particles.forEach((p, i) => {
                p.update(); p.draw();
                for(let j=i; j<particles.length; j++) {
                    let dx = p.x - particles[j].x; let dy = p.y - particles[j].y;
                    let dist = Math.sqrt(dx*dx + dy*dy);
                    if(dist < 100) {
                        // Dynamic connection color based on particle color
                        ctx.beginPath(); 
                        ctx.strokeStyle = `rgba(148, 163, 184, ${0.3 - dist/400})`;
                        ctx.lineWidth = 0.5; ctx.moveTo(p.x, p.y); ctx.lineTo(particles[j].x, particles[j].y); ctx.stroke();
                    }
                }
            });
            requestAnimationFrame(animate);
        }
        resize(); animate();

        // --- TERMINAL & CHAT ---
        const termInput = document.getElementById('term-input');
        const termOutput = document.getElementById('term-output');
        const commands = {
            'help': 'Commands: <span class="text-amber-600">about, skills, contact, clear</span>',
            'about': 'Tauqeer Mustafaa. Security Specialist & Financial Auditor.',
            'skills': 'Cyber Security, Auditing, Risk Mgmt, Python, Excel.',
            'contact': 'Email: tauqeer@example.com'
        };
        termInput.addEventListener('keydown', (e) => {
            if (e.key === 'Enter') {
                const input = termInput.value.toLowerCase().trim();
                const div = document.createElement('div');
                div.className = 'mb-2';
                div.innerHTML = `<div class="mb-1"><span class="text-green-600">➜</span> <span class="text-blue-600">~</span> ${input}</div>`;
                if(input === 'clear') termOutput.innerHTML = '';
                else if(commands[input]) div.innerHTML += `<div class="text-slate-600 pl-4 border-l-2 border-slate-200">${commands[input]}</div>`;
                else if(input) div.innerHTML += `<div class="text-red-500 pl-4">Command not found.</div>`;
                if(input !== 'clear') termOutput.appendChild(div);
                termInput.value = '';
            }
        });

        // Chat UI
        const fab = document.getElementById('ai-fab');
        const modal = document.getElementById('ai-modal');
        const close = document.getElementById('close-chat');
        fab.addEventListener('click', () => modal.classList.add('active'));
        close.addEventListener('click', () => modal.classList.remove('active'));
        
        async function callGemini(text) {
            // Mock response for visual demo as key is empty
            return "This is a simulated AI response in the White Theme. The API key is set to empty."; 
        }
        
        document.getElementById('send-chat').addEventListener('click', async () => {
            const input = document.getElementById('chat-input');
            const msgs = document.getElementById('chat-messages');
            if(!input.value) return;
            
            const userMsg = document.createElement('div');
            userMsg.className = 'bg-amber-100 p-3 rounded-lg text-sm text-slate-900 max-w-[85%] self-end mb-2 ml-auto';
            userMsg.innerText = input.value;
            msgs.appendChild(userMsg);
            
            const txt = input.value;
            input.value = '';
            
            const aiMsg = document.createElement('div');
            aiMsg.className = 'bg-slate-100 p-3 rounded-lg text-sm text-slate-700 max-w-[85%] self-start mb-2';
            aiMsg.innerText = "Processing...";
            msgs.appendChild(aiMsg);
            
            // Simulating API call
            setTimeout(() => {
                aiMsg.innerText = `I can help you with details about "${txt}" regarding Tauqeer's portfolio.`;
            }, 1000);
        });
    </script>
</body>
</html>
